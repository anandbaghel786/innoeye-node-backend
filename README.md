# innoeye-node-backend

# to run backend code 
# 1. install nodemon using command -   npm install -g nodemon
# install all packages using command -  npm install
# then run command in terminal -            nodemon start
